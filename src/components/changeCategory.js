import React from 'react';

const ChangeCategory = (props) => {
    return (
        <div>
            <input type='text' placeholder='검색하지마' />
            <button>검색</button>
        </div>
    );
};

export default ChangeCategory;